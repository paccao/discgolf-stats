{
  "name": "discgolf-stats-backend",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "start": "fastify start -l info index.ts",
    "dev": "fastify start -w -l info -P index.ts"
  },
  "dependencies": {
    "@fastify/autoload": "^5.0.0",
    "@fastify/sensible": "^5.0.0",
    "fastify": "^4.26.1",
    "fastify-cli": "^6.3.0",
    "fastify-plugin": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.4.0",
    "typescript": "^5.5.4"
  }
}